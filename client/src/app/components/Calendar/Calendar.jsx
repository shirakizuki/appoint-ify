import React from 'react'

const Calendar = () => {
    const daysOfWeek = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'];
    const currentDate = new Date();

    // GENERATE ALL MONTHS AND DAYS

    return (
        <div>

        </div>
    )
}

export default Calendar
